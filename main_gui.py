"""
Journal Swiper 
---------------
Utility for grabbing full issues of a journal from a website and stitching them 
into a single PDF file output so that you can read through it as if you had the
hard copy version. 

@author: GlocktopusPrime
@version: 0.1
"""

import sys;


def main ():
    """
    The main code, starts the UI.
    
    Inputs: None
    Outputs: None
    
    Returns: 1 on error, 0 otherwise.
    """
    try:
       # Code goes here.
        a = 3;
    except:
        # Error Handling
        
        return 1; # Exit on error
    else:
        return 0;
    
if __name__ == '__main__':
    sys.exit(main(sys.argv));